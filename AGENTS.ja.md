> **このファイルは `AGENTS.md` の日本語訳です。**
> 直接編集しないでください。内容の変更が必要な場合は canonical な `AGENTS.md`（英語）を更新し、そのうえでこの日本語訳を同期してください。
> エージェントが読むのは `AGENTS.md` だけです。このファイルは規約本体ではなく、人間が読むための翻訳です。

# エージェント向け文書

このリポジトリで作業する **AI コーディングエージェント**（Claude Code / Codex / Copilot / Gemini 等）のためのリポジトリ規約。

このリポジトリは **Next.js / React の表現層ボイラープレート**である。表現層だけを受け持ち、PaaS または静的 CDN へ配送し、バックエンド（DB / 認証 / ビジネスロジック）は別リポジトリ・別サービスであることを前提とする（[0011](docs/adr/0011-no-docker.md)）。[README.md](README.md) がこのリポジトリは何かを述べ、このファイルはその中でどう作業するかを述べる。**アーキテクチャ・規約・流れをここに再掲しない** —— どの文書が何を持つかは *正典となる文書* の表が言う。これは人間が読むための参考訳であり、canonical は [AGENTS.md](AGENTS.md) で、エージェントが読み込むのもそちらだけである。

すべての作業に、3 つの制約が掛かる。

1. **決定的な検査が在るなら、それがあなたの判断より上位に立つ** —— テスト、lint、ゲート、CI の実行、
   アーキテクチャ規則。結論したことではなく、それが何と言ったかを報告し、行や件数を落とすフィルタ越しに
   報告しない（[0157](docs/adr/0157-inspection-declaration-discipline.md)）。
2. **アーキテクチャと方針の決定は、人のゲートを残す。** 決定と選択肢を差し出すのであって、選ばない。
   どこがそれに当たるかは *手を止めてよい場所* が閉じた一覧として持ち、その外では決めて、決定を PR へ
   記録する。
3. **アプリケーションは AI に依存しない。** 実行時・ビルド・テスト・通常の CI 検査は、エージェントが
   1 つも居ない状態で、そして `.claude/` が無い状態で通らなければならない。`pnpm build` / `pnpm test` /
   必須チェックの経路に自分を置く道具は、ここには入らない
   （[0162](docs/adr/0162-application-independence-from-ai.md)）。

## v1.0.0 までの暫定運用ルール

> **暫定セクション —— v1.0.0 を出すときに削除すること**
>
> プロセスの正は [docs/plan/v1-implementation-plan.md](docs/plan/v1-implementation-plan.md)。

v1.0.0 未満の間、以下を**一時的に解除する**。

- **保護対象ドキュメントを直接編集してよい** —— `AGENTS.md` / Accepted な ADR 本体 / `LICENSE`。変更ごとの承認は要らない
- **AI の変更範囲にある保護パスを解除する** —— `package.json` / `tsconfig.json` / `next.config.ts` / `mise.toml` / `biome.json` / `Makefile` / `.makefiles/` / `.github/` / `.claude/`
- **ADR は living document であり、その場で上書きする**（[0140](docs/adr/0140-documentation-operations.md)）
- **変更履歴や経緯の残滓を本文へ残さない** —— 決定はいまの形だけを書く。履歴は git が持つ

これに合わせ、`.claude/settings.json` は `AGENTS.md` / `LICENSE` / 自分自身を `permissions.deny` ではなく `permissions.ask` に置き、Accepted な ADR 本体は権限エントリを持たない。

**解除しないもの**: 下記の Git ルールと、`permissions.deny` に載っているものすべて。

これを元に戻すのは 1 つの変更であり、**このファイルはその手順を持たない** —— 契機と手順は [0140](docs/adr/0140-documentation-operations.md) 決定 4、権限側は [0152](docs/adr/0152-agents-md-policy.md) の「v1.0.0 での復元手順」。

## 指示の優先順位

以下の順に従う。衝突したら、優先度の高い文書が勝つ。

1. **AGENTS.md**（このファイル）—— リポジトリ全体の運用規約
2. **`docs/adr/*.md`** —— 確定したアーキテクチャ決定（ADR）
3. **`.github/copilot-instructions.md`** その他エージェント固有の設定
4. ユーザの指示

<!-- boilerplate-only:begin -->
## 何を推奨するか

定めるのは**推奨**であって、変更してよい範囲ではない —— それは上記 `指示の優先順位` と下記
`AI の変更範囲` が決める。

- **新しいリポジトリが受け取るスナップショットに対して比べる。** そこへ至った履歴に対してではない ——
  このリポジトリを見たことがなく、git log を読むこともない人にとって何が筋の通った形に見えるか。
- **品質と一貫性が、そこへ到達する費用より上に立つ。** 自ら教えている順序と食い違う採番、ここ以外の
  どこでも守られている規約、改名が手間だから生き残っただけの名前 —— 直すことを推奨する。「もう出荷済み」
  はほとんど重みを持たない。
- **推奨には費用を添える** —— 触るファイル、誰の何が壊れるか、何を作り直すか。人が方向は保ったまま
  範囲だけを断れるようにするためである。
- **権威を持つのは 2 つだけ**: デファクトスタンダード（RFC・仕様・プラットフォーム自身の定義）と、この
  リポジトリのアーキテクチャが導く形。そのどちらとしても述べられない推奨は、推奨の服を着た好みである。
- **特定のデプロイの事情を、残る側へ焼き込まない。** ただしこれは上の規則に従属する —— つまみが正当化
  されるのは、その変動が本当に状況依存であり、**かつ**標準にもアーキテクチャにも決着していないときだけ
  である。標準が既に決めた場所につまみを置くのは標準からの逸脱であり、
  [0010](docs/adr/0010-standards-and-non-lockin.md) が要求する宣言を伴うか、消える必要がある。
- **判定基準は「抽象を増やす / 減らす」では決してない** —— 標準またはアーキテクチャが導く形へ寄せ、
  状況のラベルを落とす。
<!-- boilerplate-only:end -->

## 正典となる文書

**規約をここに再掲しない。** 変更が触れる領域を持つ索引を開き、そこが名指す項目を読むこと。索引を自分の
機能の語で検索するだけでは足りない —— 文書は、それを見に行かせた機能ではなく、それが所有する関心に
ちなんで名付けられている。

| 必要なもの | 読む先 |
| --- | --- |
| 確定した決定の全件、1 行要約つき | [`docs/adr/README.md`](docs/adr/README.md) —— ADR の台帳であり、一覧が在るのはここだけである |
| すべての変更を縛る規約 —— 層境界、データ分類、フォーム、コメント、作業の進め方 | [`docs/rules.md`](docs/rules.md) |
| 個別のケースをどう決めるかの基準 —— 描画、データ取得、認証、フォーム、可観測性 | [`docs/design/README.md`](docs/design/README.md)。索引を開くこと。ここに挙げた例は目録ではない |
| **Next.js 16 / React 19 は学習データと食い違う** | [`docs/design/rendering.md`](docs/design/rendering.md) —— 用語と、古い前提が招く誤り。`"use client"` は**バンドル境界**であって「クライアントで描画せよ」ではない。コードを書く前に `node_modules/next/dist/docs/` を読む |
| テストの規約 | [`docs/testing-conventions.md`](docs/testing-conventions.md) |
| 層ごとの責務と import 境界 | 触っている層の `README.md`（`src/**` 配下） |
| 画面要件・機能要件 | [`docs/spec/`](docs/spec/README.md) |
| `make` の全ターゲット | [`.makefiles/README.md`](.makefiles/README.md) |
| ある記述をどの文書が所有するか、そして開発の経緯はどこへ行くか | [`docs/README.md`](docs/README.md) |

**正典はサフィックス無しのパスである。** `*.ja.md` は読まない —— あれは正典に追従する人間向けの翻訳で
ある —— し、`docs/portal/**` も読まない —— あれは生成器が正典から書き直す生成ビューである
（[`docs/README.md`](docs/README.md)）。

## 作業の進め方

変更に着手する前に:

1. **触ろうとしているディレクトリを所有する `README.md` を読む。** そのディレクトリが持たないなら直近の
   祖先まで遡る。そこが宣言する責務と `imports-allowed` が変更の範囲を縛り、アーキテクチャのゲートが
   検査できる以上のことを述べている。
2. **上の索引を開き、変更が触れる決定を所有する項目を読む** —— 何が決まったかは `docs/adr/README.md`、
   個別のケースをどう決めるかの基準は `docs/design/README.md`。
3. **既存の実装が既にそれを覆っていないかを確かめる。** まず同じ層を探し、新しく作るより既存のファイルを
   直すほうを選ぶ。新設が正しいときは `pnpm gen` が置き場を決める。
4. **契約を、そこから生成されるコードより先に動かす。** API の変更は `openapi/` を編集して生成し直す
   （[0072](docs/adr/0072-api-type-generation.md)）。生成されたクライアントは手で直さない。画面の変更は
   仕様書を伴う —— いつかは `docs/rules.md`「テスト」が持つ。

5. **その操作を所有するスキルがあるなら、手順を組み直さずそれを起動する。** コミット・PR の作成・マージの
   解決にはそれぞれスキルが在り、**hook の扱いと検証の順序はその手順の側が持つ**ので、手で組み直した等価物は
   それらを黙って落とす。

1 と 2 は省略してよい手順ではない。**読まなかった規約も変更を縛る。**

## レビュー段階のプロトコル

実装済みの成果へのレビュー依頼が名指しているのは、**3 つ**の主題であって 1 つではない。

| スキル | 主題 |
| --- | --- |
| `/impl-review` | 変更そのもの —— 正しさ / セキュリティ / アーキテクチャ / 凝集 / 実行時の乖離 |
| `/test-review` | その変更を押さえるテスト |
| `/comment-sweep` | 変更が触れたファイルが抱えるコメントの蓄積 |

- **黙って 1 つを選ばない。** すでに手元にある文脈から各スキルの見返りを見積もる —— どの層が動いたか、
  テストやコメントがそもそも動いたか、先に走ったスキルが何を覆ったか。そのうえで**スキルごとに、その
  見積もりと理由を述べて問い**、承認されたものを回す。
- **「三つとも回しますか」に相当する問いをしない。** 費用を値札なしに突き返している。どのパスが見合うと思うか、
  どれが何も返さないと思うか、そしてなぜかを述べる。
- **3 つは対等であり、どれも他を呼び出さない。** 1 つの主題に 1 つのスキルで、そのスキルだけがその主題を
  監査する。`/impl-review` はテストの視点もコメントの視点も持たず、何も引き継がない。他の 2 つは
  `/impl-review` が回るかどうかに関わらず、それ自身として起動される。
- **これはパイプラインの内側でも変わらない** —— issue から merge 済みの PR まで駆動するスキルも、
  レビューの段でこの 3 つを利用者へ問うのであって、代わりに選ばない。

### レビューへの応答そのものは未レビューである

指摘に答えたコミットは新しく、未監査の作業であり、それを捕まえたはずのパスは、誰もが既に支払い済みだと
見なすパスである。

- **修正ラウンドは新しい範囲として宣言する**: `<レビューの最終コミット>...HEAD` であって、元の差分でも
  ブランチ全体でもない。
- **どの指摘に答えたかを述べ**、応答が実際に触れた主題のスキルだけを回し直す —— コメントを言い換えた
  だけの修正は `/impl-review` を開き直さないし、制御フローを変えた修正は開く。
- 上の見積もりの規則は引き続き効く。**「レビューはもう済んだ」はレビューされたコードについての言明で
  あって、それを置き換えたコードについての言明では決してない。**

## 禁じられた近道

**このファイルはそれを列挙しない。** 機械で決まるものはゲートが捕まえる —— biome と ESLint、
`pnpm check:architecture` と `eslint-plugin-boundaries`、カバレッジの閾値、pin とマーカーの検査、
`.claude/settings.json` の `permissions.deny`。残りは、触っている層の `README.md` と
[`docs/rules.md`](docs/rules.md) が述べる。

**このファイルが繰り返さない規則も、規則である。** ここに禁止が見つからないことは、このファイルについての
証拠であって、その禁止についての証拠ではない。

**責務の規約が 2 つ、ゲートを 1 つも持たない。** どちらもコードの形からは決まらず、`docs/rules.md` は
それぞれの箇所でそう述べている。自分で持つこと。

- **他の層が握る問題を、こちらで予防的に手当てしない。** 書かないのは「下の層が既に握っているもの」と
  「起こり得ないもの」で、「下では捕まえられないもの」「UX 上こちらに在るべきもの」は対象外。
  **セキュリティ上の懸念は重複を理由に落とさない。**
- **上流から来た値を、網羅的に無害化しようとしない。** 表示層が始末するのは**自分が作った値**であり、
  バックエンドが載せた文字列・パス・識別子を全面的に安全化することは設計目標ではない —— 網羅は達成できず、
  供給側の都合が表示側の構造へ染み出す。防ぐべきものは供給側か境界で閉じる。

## 手を止めてよい場所

**判断をユーザへ返してよい場所は一覧であって、判断ではない。** そしてその一覧は閉じている。外では、決め、
実行し、決めたことを PR 本文へ記録する。

### 停止点

それぞれの持ち主は列に書いた文書である。このセクションは索引であり、中身は繰り返さない。

| 停止点 | 持ち主 |
| --- | --- |
| amend 後に既存 PR ブランチへ push する | 下記 *Git ルール* —— その文面をそのまま使う |
| `redirect.github.com` ではなく素のリンクで他リポジトリを参照する | ADR [0159-1](docs/adr/0159-1-cross-repository-references.md)。**毎回、案件ごとに** —— 恒常的な委任はこの権限を移さない |
| スキルがこれから行う外向き・商用のアクション | ADR [0154](docs/adr/0154-claude-skills-operations.md) |
| ユーザに見えている要素を減らす変更 | `docs/rules.md`、*作業とエージェント* |
| 依存を追加する | ADR [0004](docs/adr/0004-library-management.md) —— その選定基準を辿り、採用判断のテンプレを PR へ貼る。黙って依存を迂回するのも同じ決定であり、記録だけが無い形になる |

### トリップワイヤ

これらは**あなたの判断がどうであれ**作業を止める。続けること自体が誤りになる。

1. **次の手順が `permissions.deny` の操作を要する。** 別のインタプリタ越しに回すのは解決ではない。
   deny 一覧を編集するのも解決ではない。
2. **次の手順が履歴を書き換える、または保護ブランチへ触れる** —— force push、rebase、amend からの push。
3. **次の手順が生成物を編集する。** `git check-attr linguist-generated -- <path>` が任意のパスに答える。
   生成バナーも同じことを言う。
4. **権威を主張する 2 つの出所が食い違っている。** 気づくことが仕事で、解決することは仕事ではない
   （`docs/rules.md`、*作業とエージェント*）。
5. **その変更によって、文書が検査できないことを主張することになる** —— 持ち主のいない規則、評価器のない
   主張（[0157](docs/adr/0157-inspection-declaration-discipline.md)）。

### それ以外のすべて

決め、そして**決定を PR 本文へ書く** —— 何を選び、何を選ばなかったか。記録された決定は読み手が覆せるが、
黙って取られた決定は再導出しない限り発見されない。

## AI の変更範囲

既定では、AI エージェントが変更してよいのは以下の範囲だけである。それ以外のパスはユーザの明示的な指示を要する。

> v1.0.0 未満では、このセクションの保護パスは解除されている —— 上記「v1.0.0 までの暫定運用ルール」を見よ。

### 許可

- `src/` の下
- `public/`（アセットの追加）

### ユーザの指示なしに触らない

- リポジトリルートの設定ファイル: `package.json` / `pnpm-workspace.yaml` / `tsconfig.json` / `next.config.ts` / `mise.toml` / `biome.json` / `postcss.config.mjs` / `Makefile` など
- `.makefiles/`（リリース / ブランチ操作の make ターゲット）
- `.github/`（ワークフロー / 設定 / issue・PR テンプレート）
- `LICENSE`
- Accepted な ADR 本体（`docs/adr/0001-*.md` 以降、Status: Accepted のもの）

### エージェント設定ファイルの保護

エージェントの設定は、そのエージェント自身であっても触ってはならず、変更にはユーザの明示的な指示を要する。**その保守は例外ではなく標準経路に含まれる** —— それを所有するスキル（スキルとエージェント定義は `manage-skill`、対訳は `canonicalize-doc`）を通して到達し、そのスキルの起動が指示を供給する。エージェント自身の発意では決して行わない。

- Claude Code: `.claude/`（`.claude/skills/` / `.claude/settings.json` / `.claude/settings.local.json` など）
- OpenAI Codex CLI: `.agents/skills/`
- Cursor: `.cursor/`（`.cursor/rules/*.mdc` を含む）/ `.cursorrules`
- GitHub Copilot: `.github/copilot-instructions.md` / `.github/instructions/` / `.github/prompts/`
- Gemini CLI / Code Assist: `.gemini/` / `GEMINI.md`

`AGENTS.md` は全エージェント共通の**保護対象ドキュメント**である。

### 例外: スキル実行

スキルの起動（Claude Code の `/<skill-name>` など）は**明示的な指示**として数える。実行中、この範囲はそのスキルの `SKILL.md` が宣言する範囲へ緩められる。

- 緩和は**スキルの実行中だけ**、宣言された範囲だけで、スキル自身の確認手順は引き続き効く
- **スキル実行中も強く保護される**: `AGENTS.md`、Accepted な ADR 本体、`LICENSE`、`permissions.deny` に載っているもの
- スキルを抜け道にしない。手順が機微な領域（`.github/workflows/`）に触れるなら、起動時に利用者が気づけるよう `SKILL.md` が宣言する

## 物を入れるとき

対象はあらゆる `install` の口である: パッケージマネージャ（`brew` / `pnpm add -g` / `pip`）、
ツールチェーンマネージャ（`mise use -g`）、IDE・エージェントの統合（`<tool> install`）、プラグイン、拡張。

- **自分の判断で入れない。** 道具を*使いたい*ことは、道具を*入れたい*という指示ではなく、道具が無いことは
  報告すべき所見である。とくにエージェントの統合は project スコープの指示ファイル（`CLAUDE.md` /
  `AGENTS.md` / `.cursor/` / `.gemini/` / git hook）を書き、「インストール」がいま従っている規約そのものへの
  編集になる。
- **その前に、いまの構成が何を持っているかを見る。** ツールチェーンは [`mise.toml`](mise.toml) に pin され
  `make install-tools` で入るので、必要な能力はたいてい既に `make` のターゲットの向こうに在る。無いと
  結論する前に、何を確かめたかを述べる。
- `allow` に載っているのはプロジェクト内で完結する冪等なインストールだけなので、それ以外は既にすべて人の
  判断へ出る。*依存*を足すことはより厳しい別の問いである —— [0004](docs/adr/0004-library-management.md)、
  そして停止点の 1 つ。

## 推奨コマンド

すべてのコマンドを**素で実行する。`mise exec -- <command>` は禁止**である —— 自分が打つものでも、
`.lefthook.yaml` でも、`.makefiles/` のレシピでも、どこでも（[0003](docs/adr/0003-version-manager.md)）。
素のコマンドが mise の外へ解決されるなら、直すのは `PATH` であってラッパーではない（症状は `repo-ops`）。

### pnpm（ADR 0001）

**script の一覧は `package.json` が持ち、日常的に手が伸びるものは [README.md](README.md) が名指す。**
そこから導けないものが 3 つある。

- **コア依存・主要な開発ツールには `pnpm add -E`** —— [0004](docs/adr/0004-library-management.md) が版の
  固定を要求している。そもそも依存を足すこと自体が停止点である。
- **`pnpm gen <kind> <name>`** は feature / component / adapter を
  [0027](docs/adr/0027-directory-structure.md) / [0028](docs/adr/0028-naming-convention.md) が要求する形で
  生成する。
- **`pnpm lint` は biome だけ**で、`lint:ci` が ESLint と境界の突合を足す。hook と CI が回すのは後者。
  **`pnpm lint` が緑でも `lint:ci` は落ちうる** —— react hooks の規則（render 中の ref 書き込み・effect 内の
  `setState`）と型アサーションの禁止は ESLint 側にしか無い（[0002](docs/adr/0002-formatter-linter.md)）。
  手元で確かめるなら `pnpm exec eslint <path>`。

### make（ブランチ操作 / ゲート / レビュー）

**完全な登記簿は [`.makefiles/README.md`](.makefiles/README.md)** で、`make help` がレシピから同じ一覧を
出す。リリース・タグ・hotfix・一度きりの導入はそちらに在り、ここには置かない。

```bash
make base-branch           # origin から読んだ最新のリリースラインを出力する（ADR 0150）
make base-merge            # そのベースを現ブランチへマージし、未解決のパスを出力する
make load-status           # いまのゲート帯とその理由を表示する（ADR 0151）
make vrt-review            # CI が指摘した story を使い捨て worktree で開く
make e2e-review            # CI が指摘した画面向け。dev サーバではなく本番ビルドを起動する
```

#### git worktree の中で作業するとき

複数の worktree が 1 台のホストへ並び、**ポートを割り当てる機構は無い。** 待ち受けるもの ——
`pnpm dev` / `pnpm start` / `make e2e` / `make lighthouse` / `make vrt-review` —— はいずれもポートを
明示で取る（`E2E_PORT` / `VRT_REVIEW_PORT` / `--port`）。既定に任せず、他の worktree が掴んでいない番号を
選び、別の checkout が立てたサーバを乗っ取らない。レビュー用ターゲットが `tmp/review/` に残す使い捨ての
worktree は `make review-clean` が片付ける —— Ctrl-C では消えず、それぞれ `node_modules` を抱えたまま残る。

#### すべてのターゲットは `make ai-<target>` で叩く

`make ai-trivy-fs` は `make trivy-fs` を実行し、出力を丸ごと `tmp/ai-logs/trivy-fs.txt` へ落とす。成功時は
無言、終了コードは透過、失敗時だけ 1 行でログを名指しする。**これは既定であって、どのターゲットが
騒がしく見えるかの判断ではない。** 例外は閉じた一覧である: 出力そのものが答えのターゲット（`help` /
`load-status` / `base-branch` / `lighthouse-report`）と、戻ってこないターゲット（`e2e-report` /
`vrt-report`）。`make clean-ai-logs` がディレクトリを空にする。

#### CI の実行は `--log` ではなく `--log-failed` で読む

このリポジトリの Test ワークフローで実測すると、`--log` は **1.6 MB / 約 450,000 トークン**、同じ実行の
`--log-failed` は **1,959 バイト**で失敗の全体を運んでいた。常に `--log-failed` を先に使い、失敗が本当に
失敗ステップの中に無いときだけ `--log` へ手を伸ばし、何で絞ったかを述べる。**ゲートの判定を欠損する
フィルタ越しに報告しない**（[0157](docs/adr/0157-inspection-declaration-discipline.md)）。

<!-- boilerplate-only:begin -->
### ゲートを先回りして回さない

**このセクションはテンプレートから作られたリポジトリからは削除される。** あちらは作業ツリーが 1 つなので
先回りは安い。こちらでは複数の worktree が 1 台のホストへ並び、ゲートは並ぶのではなく重なる。

**hook と CI があなたの代わりに回し、判定は CI が正である**（[0151](docs/adr/0151-git-hooks.md)）:
`pre-commit` は lint 一式とキャッシュ済みテストを、`pre-push` はさらに型検査・全テスト・秘密情報スキャンを
回す。

- **commit し、push し、hook か CI から判定を読む。**
- いま編集した 1 ファイルを回し直すのは構わない。スイート全体や lint 全体を掃くのは違う。
- `make load-status` はいまローカルで回るゲートを表示し、ホストが混んでいるときは重いものが自動的に CI へ
  委譲される。**`--no-verify` で先回りして潰さない** —— 迂回は 0151 の方針が決めるのであって、ゲートが
  遅く感じるかどうかではない。
<!-- boilerplate-only:end -->

### 文脈へ届くものだけを変える 2 つの道具

どちらも `pnpm build` / `pnpm test` / 必須チェックの経路には居ない（制約 3）。どちらも
[`mise.toml`](mise.toml) で pin している。費用と除外は [`.claude/README.md`](.claude/README.md) が持ち、
ここに置くのはこれが毎ターン効く規律だからである。

**`rtk`** はコマンドの出力を、あなたへ届く前に圧縮する（`rtk <サブコマンド> <元のコマンド>`）。

- **包める経路は原則これを通す。** 圧縮できないコマンドは素通しになるので、呼ぶたびに量る必要は無い ——
  実測で `rtk git diff` は 10x、`rtk find` は 64x、`rtk git log` は素通しだった。量るのは効き目ではなく
  欠損である。
- **`grep` と `read` は包まない。** `rtk grep` は小さい対象では素通しだが、**大きい対象では黙って打ち切る**。
  `read` は既定より上のすべての level で行を落とす。どちらも**欠けているのに完全に見える出力**を作る。
- **ゲート・テスト・lint の結果をこれ越しに報告しない。** `rtk log` と `rtk read -l` はその理由で `deny`
  に在る。この種の規則が住むべき場所がそこである（[0144](docs/adr/0144-decision-enforcement-pairing.md)）。
- 任意の内側コマンドを実行する形（`run` / `summary` / `smart`）も `deny` である。内側のコマンド粒度で
  書かれた allow を迂回するためである。

**`graphify`** はこのリポジトリをローカルで AST 解析した知識グラフである（`/graphify`）。

- **使ったなら、その鮮度を述べる。** グラフは最後の `update` 時点のスナップショットで、未コミットの作業には
  盲目である。`git rev-parse HEAD` と突き合わせ、答えにそう書く。
- **グラフはファイルへ到達する手段であって、根拠そのものではない。** 指された先を開き、そちらを引用する。
- **安いほうの既定ではない。** 狙って書いた `grep` に対し、小さな差分では実測 0.76x〜3.8x 悪化した。効くのは
  `affected` である。`query` はトークン budget で切り詰めるので、網羅性が要る問いには答えない。
- LLM を呼ぶサブコマンドは内容をマシンの外へ出すので opt-in のままにする。

## Git ルール

[0150](docs/adr/0150-git-workflow.md) が正である。ここには要点だけを置く。

### 厳守事項

- **force push・rebase・amend・保護ブランチの checkout は、非推奨ではなく `.claude/settings.json` の
  `deny` で塞いである。** 修正は**新しいコミット**として積み、deny を迂回しない。
- 既存 PR ブランチでコミットを amend したら、**push 前にこの文面で確認する**: 「変更はローカルにコミット済みです。これらの変更をプルリクエストにプッシュしますか？」

### ブランチ・コミット・プルリクエスト

- **分岐元は `$(make -s base-branch)`。** それ以外の出所はどれも、そう名乗らないまま古い —— ローカルの
  `origin/HEAD`、GitHub のデフォルトブランチ、ハーネスが渡す「Main branch」の値、いずれも同じである。
  名前は `feature/` / `bugfix/` / `hotfix/` + issue 番号が在れば `<issue-no>-` + kebab の説明。リリース
  ラインは `release/v<X.Y.Z>`。解決器が答えない場合が 2 つある: **既にある PR の `baseRefName` が正**で
  あり、**hotfix の分岐元は人の判断なので聞く**
  （[`scripts/base-branch/README.md`](scripts/base-branch/README.md)）。
- **追いつかせるのは `make base-merge` であって rebase ではない。** 取り込むのはそのブランチを切った元の
  ベースで、今日 `base-branch` が返すものではない —— 後者は追いつかせるのではなく行き先を付け替える。
  そもそも取り込むかどうかは `docs/rules.md`「作業とエージェント」が持つ判断である。
- **コミットの件名は `<Prefix>: <日本語の件名>`** で、末尾に `。` を付けない。prefix の集合は
  [`commitlint.config.ts`](commitlint.config.ts) が `commit-msg` hook を通して検証する —— 写しではなく
  そちらを読む。
- **PR のタイトルは日本語**で、本文は [`.github/pull_request_template.md`](.github/pull_request_template.md)
  の `概要` / `変更内容` / `動作確認方法` を埋める。既定のマージ戦略はマージコミット。既存 PR への push は
  過去の approve を自動で無効化するので、push 後は再レビューを依頼する。

## 言語ルール

**内部処理は英語でよい** —— コード解析、アーキテクチャの推論、ツールの呼び出し、その途中のすべて。
**書き戻すものは日本語である。** ユーザが明示的に別を指示した場合を除き、そしてその指示は立っているあいだ
有効である。

### 出力言語

対象:

- ユーザへの応答
- コードコメント
- PR のタイトルと本文
- コミットメッセージ
- ドキュメント
- テストの `it` 文字列。最外の `describe` はエクスポートされたシンボル自身の名前なので、ソースのまま置く（[0090](docs/adr/0090-testing-strategy.md)）
- AI が生成するインラインドキュメント

技術用語（HTTP ステータスコード名 / API 名 / コマンド名等）は英語のままでよい。

**例外 —— ワークフロー定義（`.github/workflows/**` と `.github/actions/**`）のコメントは
英語で書く**（[0140](docs/adr/0140-documentation-operations.md)）。`.github/` のそれ以外 ——
issue / PR テンプレート、`settings/`、道具自身の設定 —— は日本語ルールに従う。

### 応答の規律

定めるのは書き返す内容であって、してよいことではない。上のどの規則も緩めず、簡潔さがこのファイルの要求する
確認を省く理由になることは無い。

- **結論を先に。** まず結果、次に自明でないところだけ理由。前置きも、依頼の言い直しも、締めの要約も要らない。
- **読んでいない検証可能な事実を書かない。** API 名・フラグ・バージョン・パス・シンボル・コミット SHA・
  パッケージ名は、先にコードか文書を開く。「確認できていない」は答えであり、それらしく見える捏造は
  答えではない。
- **聞かれた範囲と、それを塞いでいるものを報告する。** 気づいた周辺のことは 1 行か issue であって、
  頼まれていない節ではない。
- **決定的な検査は、それ自身が報告したとおりに報告する** —— 行・件数・網羅率を落とすフィルタ越しに
  中継しない（[0157](docs/adr/0157-inspection-declaration-discipline.md)）。
- **生成物に装飾的な Unicode を持ち込まない。** コード・設定・コミットメッセージは素のハイフンと直線引用符
  を使う。人間向けの散文は通常の組版を保つ。

<!-- boilerplate-only:begin -->
## 純化パス

このリポジトリのファイルはすべて一度だけ歩かれる。`PreToolUse` hook が編集前にパスを引き、まだ記帳されて
いなければ手順の在り処を告げる。**決してブロックはしない。**
[`.agents/README.md`](.agents/README.md) が機構・台帳・照会の入口を持つ。

このパスは**ファイル全体**に対して 3 つを問う。あなたの差分に対してではない。

1. **純度** —— このテンプレートから作られたリポジトリは、ここにあるすべての参照を解決できるか。そして
   このリポジトリがテンプレートになったとき、すべての記述はなお真であるか。
2. **蒸留** —— このファイルはどんな設計判断を体現しているか。
3. **配置** —— その判断を持つべき文書はどれか: ADR、層の `README.md`、feature の `README.md`、
   `docs/rules.md`、あるいはコードそのもの。

- 判定基準は `.agents/purity-sweep/purity-sweep.prompt` に在る。hook がそう言ったら読む。
- **ファイルを全部見終えてから記帳する** —— 起きていないパスを主張する記帳は、記帳が無いより悪い。その
  ファイルを二度と誰も見なくなるからである。
- ファイル編集ツールではなく `Bash` 経由の編集は hook を起こさないが、この規則はそれにも等しく効く。

これはレビューの車線ではない。`レビュー段階のプロトコル` が判ずるのは**変更**であり、こちらが判ずるのは
**ファイルの蓄積された状態**である。ファイルに触れた副作用として走る。
<!-- boilerplate-only:end -->

## 保護対象ドキュメント

以下は変更前に人間の意図的なレビューを要する。

- `AGENTS.md`（このファイル）
- Accepted な ADR 本体（`docs/adr/0001-*.md` 以降、Status: Accepted のもの）
- `LICENSE`

直接編集しない —— 変更案を提示し、明示的な承認を得てから編集する。新しいファイルが必要に見えるときでも、
既存のファイルで足りるなら**そちらを変更する**ほうを選ぶ。

> v1.0.0 未満ではこの承認要件は解除されている —— 上記「v1.0.0 までの暫定運用ルール」を見よ。
