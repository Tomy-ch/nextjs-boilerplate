# `/about` このサイトについて（機能要件）

> 画面要件は [`page.screen.md`](page.screen.md)。

## レンダリング

取得を持たない。内容が変わるのはコードを書き換えたときだけ
（[0040](../../../../adr/0040-routing-rendering-strategy.md)）。

## 認可

**保護の対象にしない。** 何のためのサイトかは、ログインする前に読めなければ意味を持たない。

**ただし描画は動的になる。** 外枠がカートを読むためで、この画面の都合ではない（[`../layout.function.md`](../layout.function.md)）。
