# `/terms` 利用規約（機能要件）

> 画面要件は [`page.screen.md`](page.screen.md)。

## レンダリング

取得を持たない。

## 認可

**保護の対象にしない。** 閲覧した時点で同意とみなす以上、ログインする前に読めなければ成立
しない。

**ただし描画は動的になる。** 外枠がカートを読むためで、この画面の都合ではない（[`../layout.function.md`](../layout.function.md)）。
