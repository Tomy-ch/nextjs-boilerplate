/**
 * 現在地までの階層を持たない画面。
 *
 * @remarks
 * 階層の一番上にある画面は自分より上を持たないため、何も出しません。この既定が無いと、slot に
 * 対応する route を持たない画面を直接開いたときに 404 になります。
 *
 * **この既定が効くのは読み込み直したときだけです。** 画面を跨いで移ると、slot に対応する route を
 * 持たない画面では直前の slot がそのまま残ります。階層を持たない画面にも空を返す route ごとの
 * slot を置くのはこのためで、置かないと登録を終えて一覧へ送られた先に「新規作成」の階層が残ります。
 */
export default function AdminBreadcrumbDefault() {
  return null;
}
