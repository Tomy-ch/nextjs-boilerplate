/**
 * 管理側の利用者一覧が 1 ページに並べる件数。
 *
 * @remarks
 * **契約が受け付ける上限（100）ではなく、1 画面で見比べられる量を採ります。** 上限は「これ以上は
 * 拒む」という契約の線であって、何件並べると読めるかとは別の理由で動きます。
 *
 * 商品一覧と同じ値にしてあるのは偶然ではなく、どちらも「1 ページを見比べて操作する」面だからです
 * （[`ADMIN_PRODUCT_PAGE_SIZE`](../products/list/page-size.ts)）。片方の都合で変えるときは、
 * もう片方を巻き込まないよう別々に持ちます。
 */
export const ADMIN_USER_PAGE_SIZE = 20;
