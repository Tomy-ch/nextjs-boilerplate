/**
 * 発送の送信が持つ項目の名前。
 *
 * @remarks
 * 送る側と受け取る側が同じ綴りを使うための宣言です。文字列を両側に書くと、片方だけを直したときに
 * 型では止まらず、実行して初めて「値が届いていない」形で現れます。
 */
export const SHIPMENT_FORM_NAMES = {
  /**
   * 発送する購入。
   *
   * @remarks
   * **繰り返して並べます。** 1 件ずつの発送も、組をまとめた発送も、同じ送信で受けます。契約の
   * 発送は購入 1 件ずつなので、まとめる単位を送信の形の違いで表すと、受け取る側が 2 通りになります。
   */
  purchaseCode: "purchaseCode",
} as const;
