/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Go Boilerplate API
 * Reference specification for the sample API (users, products, purchases). It exercises every layer
 * of the Onion Architecture end to end: cursor and offset pagination, creation and partial update,
 * soft delete, idempotency keys, and role-based authorization.
 *
 * Handlers (oapi-codegen) and the published reference documentation are both generated from this
 * file, so every endpoint change starts here.
 *
 * OpenAPI spec version: 2.2.0+e95da0c
 */
import type { ProductCategoryRef } from "./productCategoryRef";
import type { ProductImageItem } from "./productImageItem";
import type { ProductStatusRef } from "./productStatusRef";

/**
 * 商品情報のレスポンススキーマ
 */
export interface ProductResponse {
  /** 商品ID */
  id: string;
  /**
   * 商品名
   * @maxLength 255
   */
  name: string;
  /**
   * 商品説明。未設定の場合は null です。
   * @nullable
   */
  description: string | null;
  /**
   * 価格。サブセント精度を保持する decimal 文字列で表します（例 "19.99" は 19.99 ドル）。 JSON number は IEEE754 double として復元され精度を失うため、文字列で表現します。
   * @pattern ^\d+(\.\d+)?$
   */
  price: string;
  /** 在庫数 */
  quantity: number;
  /**
   * 在庫警告閾値。未設定の場合は null です。
   * @nullable
   */
  stockWarningThreshold: number | null;
  status: ProductStatusRef;
  category: ProductCategoryRef;
  /**
   * 公開日時。未公開の場合は null です。
   * @nullable
   */
  publishedAt: string | null;
  /** 商品画像。sortKey の昇順で返します。画像が 1 枚も無い場合は空配列です。 表示 URL はフロントが配信ベース URL と各要素の imagePath から組み立てます。 */
  images: ProductImageItem[];
  /** 楽観ロック用のバージョン。更新のたびに 1 つ進みます。 部分更新（PATCH /v1/products/{productId}）へそのまま渡すことで、 読み込み後に他者が更新していた場合の上書き（lost update）を 409 として検出できます。 */
  version: number;
}
