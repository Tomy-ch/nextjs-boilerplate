/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Go Boilerplate API
 * Reference specification for the sample API (users, products, purchases). It exercises every layer
 * of the Onion Architecture end to end: cursor and offset pagination, creation and partial update,
 * soft delete, idempotency keys, and role-based authorization.
 *
 * Handlers (oapi-codegen) and the published reference documentation are both generated from this
 * file, so every endpoint change starts here.
 *
 * OpenAPI spec version: 2.2.0+15463a1
 */
import type { AddressCandidate } from "./addressCandidate";

/**
 * 郵便番号 lookup による住所候補のレスポンスです。
 * 外部 lookup サービスの障害時は 503 ではなく `candidates: []` + `isFallback: true` で degrade し、
 * 登録フローを止めません（外部が落ちても縦一本のジャーニーを進めるためのフォールバック）。
 */
export interface AddressCandidatesResponse {
  /** 住所候補の一覧。該当なし・degrade 時は空配列。 */
  candidates: AddressCandidate[];
  /**
   * lookup 機構が機能しなかった（外部不通・不正応答）場合に true。true のときフロントは全項目手入力へ倒します。
   * 正常応答で候補が 0 件（該当なし）の場合や、候補はあるが県名解決に失敗した場合は false です。
   */
  isFallback: boolean;
}
