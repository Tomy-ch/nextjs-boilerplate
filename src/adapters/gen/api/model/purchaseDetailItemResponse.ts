/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Go Boilerplate API
 * Reference specification for the sample API (users, products, purchases). It exercises every layer
 * of the Onion Architecture end to end: cursor and offset pagination, creation and partial update,
 * soft delete, idempotency keys, and role-based authorization.
 *
 * Handlers (oapi-codegen) and the published reference documentation are both generated from this
 * file, so every endpoint change starts here.
 *
 * OpenAPI spec version: 2.2.0+8f733fb
 */

/**
 * 購入詳細（GET）専用の購入明細レスポンス。productName は products との結合で解決した現在の商品名です。
 * unitPrice は購入時点の単価スナップショットです。
 */
export interface PurchaseDetailItemResponse {
  /** 商品ID */
  productId: string;
  /** 商品名（products との結合で解決した現在名） */
  productName: string;
  /** 購入数量 */
  quantity: number;
  /**
   * 購入時点の単価スナップショット。サブセント精度を保持する decimal 文字列（USD ドル）で表します（例 "800.00"）。
   * 購入成立後に商品価格が変わっても本値は不変です。
   * @pattern ^\d+(\.\d+)?$
   */
  unitPrice: string;
}
