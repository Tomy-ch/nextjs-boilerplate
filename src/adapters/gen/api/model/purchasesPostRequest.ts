/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Go Boilerplate API
 * Reference specification for the sample API (users, products, purchases). It exercises every layer
 * of the Onion Architecture end to end: cursor and offset pagination, creation and partial update,
 * soft delete, idempotency keys, and role-based authorization.
 *
 * Handlers (oapi-codegen) and the published reference documentation are both generated from this
 * file, so every endpoint change starts here.
 *
 * OpenAPI spec version: 2.2.0+f6c9463
 */
import type { PurchaseDetailInput } from "./purchaseDetailInput";

/**
 * 購入作成リクエスト。明細（商品IDと数量）の配列を受け取ります。
 * 明細は 1 件以上必須で、同一 productId の重複は許可しません（いずれも 422）。
 */
export interface PurchasesPostRequest {
  /**
   * 購入明細の配列。1 件以上必須で、同一 productId の重複は不可です。
   * 空配列・重複はドメインで検証し 422 を返します（スキーマでは制約せず単一の検証責務に集約）。
   */
  details: PurchaseDetailInput[];
}
