/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Go Boilerplate API
 * Reference specification for the sample API (users, products, purchases). It exercises every layer
 * of the Onion Architecture end to end: cursor and offset pagination, creation and partial update,
 * soft delete, idempotency keys, and role-based authorization.
 *
 * Handlers (oapi-codegen) and the published reference documentation are both generated from this
 * file, so every endpoint change starts here.
 *
 * OpenAPI spec version: 2.2.0+e95da0c
 */
import type { PurchaseDetailItemResponse } from "./purchaseDetailItemResponse";
import type { PurchaseStatusRef } from "./purchaseStatusRef";

/**
 * 購入詳細（GET）のレスポンススキーマ。status は ID・名称を事前解決済みで、明細には商品名
 * （products との結合で解決した現在名）を含みます。paidAt は未支払いなら null、canceledAt は
 * 未キャンセルなら null です。金額（subtotalAmount / taxAmount / shippingFee / totalAmount）は
 * すべて USD セント単位の整数、明細 unitPrice は購入時点の単価スナップショット（USD ドルの decimal 文字列）です。
 */
export interface PurchaseGetDetailResponse {
  /** 購入ID */
  id: string;
  /** 購入コード（UUIDv7 文字列・一意） */
  code: string;
  /** 購入したユーザーのID */
  userId: string;
  status: PurchaseStatusRef;
  /** 小計。明細（単価×数量）の合計。USD セント単位の整数です。 */
  subtotalAmount: number;
  /** 税額（国内消費税）。USD セント単位の整数です。 */
  taxAmount: number;
  /** 送料。USD セント単位の整数です。 */
  shippingFee: number;
  /** 合計（小計 + 税額 + 送料）。USD セント単位の整数です。 */
  totalAmount: number;
  /** 購入明細の配列（商品名込み） */
  details: PurchaseDetailItemResponse[];
  /** 注文日時 */
  orderedAt: string;
  /**
   * 支払日時。未支払いの場合は null です。
   * @nullable
   */
  paidAt?: string | null;
  /**
   * キャンセル日時。未キャンセルの場合は null です。
   * @nullable
   */
  canceledAt?: string | null;
}
