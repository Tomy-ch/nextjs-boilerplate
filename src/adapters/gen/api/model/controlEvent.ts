/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Go Boilerplate API
 * Reference specification for the sample API (users, products, purchases). It exercises every layer
 * of the Onion Architecture end to end: cursor and offset pagination, creation and partial update,
 * soft delete, idempotency keys, and role-based authorization.
 *
 * Handlers (oapi-codegen) and the published reference documentation are both generated from this
 * file, so every endpoint change starts here.
 *
 * OpenAPI spec version: 2.2.0+7b2778e
 */
import type { ControlEventAction } from "./controlEventAction";
import type { ControlEventReason } from "./controlEventReason";

/**
 * レスポンス確定後にサーバから client へ送る制御指示（SSE の `event: control`）。SSE `id` を持たないため `Last-Event-ID` を汚しません。
 * client は `action` だけで分岐し、`reason` は記録用の安定値として扱います。`STOP` / `REAUTHENTICATE` / `RESYNC` は
 * サーバの切断を待たず client 側で接続を閉じてから遷移します。
 */
export interface ControlEvent {
  /**
   * client がとる動作。`RECONNECT` は通常の再接続、`RETRY_LATER` は `retryAfterMs` 後の再接続、
   * `REAUTHENTICATE` は新しい ticket を取得しての再接続、`RESYNC` は正本の再取得後の再接続、`STOP` は再接続しない。
   */
  action: ControlEventAction;
  /** 指示の理由を表す安定した値。人が読む文言は含みません。 */
  reason: ControlEventReason;
  /**
   * 再接続までの待ち時間（ミリ秒）。目安であり、client は jitter を加えてよい。レスポンス確定前の `Retry-After` ヘッダは秒単位です。
   * @minimum 0
   * @maximum 86400000
   */
  retryAfterMs?: number;
}
