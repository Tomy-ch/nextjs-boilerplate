/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Go Boilerplate API
 * Reference specification for the sample API (users, products, purchases). It exercises every layer
 * of the Onion Architecture end to end: cursor and offset pagination, creation and partial update,
 * soft delete, idempotency keys, and role-based authorization.
 *
 * Handlers (oapi-codegen) and the published reference documentation are both generated from this
 * file, so every endpoint change starts here.
 *
 * OpenAPI spec version: 2.2.0+76e4825
 */

/**
 * 購読に使う 1 回限りの ticket。生値はこの応答にしか現れず、サーバは hash しか保存しない。
 * 接続は GET /v1/streams/{destination} に streamId を渡して開始する。
 */
export interface InquiryStreamTicketResponse {
  /** ticket の生値。log や URL に残さないこと。 */
  ticket: string;
  /** 接続先の stream。GET /v1/streams/{destination} の destination に渡す。 */
  streamId: string;
  /** この ticket で新しい接続を始められる期限。確立済みの接続は別に最大接続時間で区切られる。 */
  expiresAt: string;
}
