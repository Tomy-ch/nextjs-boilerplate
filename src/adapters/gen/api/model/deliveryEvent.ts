/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Go Boilerplate API
 * Reference specification for the sample API (users, products, purchases). It exercises every layer
 * of the Onion Architecture end to end: cursor and offset pagination, creation and partial update,
 * soft delete, idempotency keys, and role-based authorization.
 *
 * Handlers (oapi-codegen) and the published reference documentation are both generated from this
 * file, so every endpoint change starts here.
 *
 * OpenAPI spec version: 2.2.0+76e4825
 */
import type { DeliveryEventPayload } from "./deliveryEventPayload";
import type { StreamCursor } from "./streamCursor";

/**
 * SSE で配送される business event の封筒。feature に依らず同じ形で、`payload` の形は `type` ごとに feature が定めます。
 * SSE の `id` には `sequence` がそのまま入ります。
 */
export interface DeliveryEvent {
  /** event の一意な識別子。同じ event が再送されても値は変わりません。 */
  eventId: string;
  /** この event が属する stream の識別子。接続先の destination と一致します。 */
  streamId: string;
  sequence: StreamCursor;
  /** feature が定める event 種別。末尾の `.vN` で契約の世代を表します。 */
  type: string;
  /** event が発生した時刻（UTC）。 */
  occurredAt: string;
  /**
   * payload の schema 世代。`type` の `.vN` と同じ値を保ちます。
   * @minimum 1
   */
  schemaVersion: number;
  /** event の本体。形は `type` ごとに feature が定めます。 */
  payload: DeliveryEventPayload;
}
