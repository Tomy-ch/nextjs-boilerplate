/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Mock Auth Server (OIDC Provider)
 * A mock OIDC Provider for local development.
 * It offers the Authorization Code Flow + PKCE (S256) in a minimal form.
 *
 * The endpoints fall into three groups:
 *
 * - `/oidc/*` — standard OAuth2 / OIDC endpoints (authorize / token / userinfo / logout).
 * - `/bypass/*` — test shortcuts (issuing a token / session for a fixed profile). dev-gate only.
 * - `/admin/*` — operational helpers (fixture reset / user listing / key rotation). dev-gate only.
 *
 * Parameters and fields are snake_case, following OAuth2 (RFC 6749) / PKCE (RFC 7636) / RFC 9068 / OIDC Core.
 *
 * OpenAPI spec version: 1.0.0+ceadce3
 */
import type { OidcConfigurationCodeChallengeMethodsSupportedItem } from "./oidcConfigurationCodeChallengeMethodsSupportedItem";
import type { OidcConfigurationIdTokenSigningAlgValuesSupportedItem } from "./oidcConfigurationIdTokenSigningAlgValuesSupportedItem";
import type { OidcConfigurationTokenEndpointAuthMethodsSupportedItem } from "./oidcConfigurationTokenEndpointAuthMethodsSupportedItem";

/**
 * OIDC Discovery 文書（OIDC Discovery）。issuer / jwks_uri は Go 側認証が依存する契約であり不変に保つ。 endpoint URL は /oidc/* を指す。
 */
export interface OidcConfiguration {
  /** 発行者識別子。token の iss と厳密一致する（不変）。 */
  issuer: string;
  /** JWK Set の URL（不変）。 */
  jwks_uri: string;
  authorization_endpoint: string;
  token_endpoint: string;
  userinfo_endpoint?: string;
  end_session_endpoint?: string;
  response_types_supported: string[];
  grant_types_supported?: string[];
  subject_types_supported: string[];
  /** 署名アルゴリズムは RS256 のみ。alg=none は発行しない。 */
  id_token_signing_alg_values_supported: OidcConfigurationIdTokenSigningAlgValuesSupportedItem[];
  scopes_supported?: string[];
  /** public client（PKCE）のみを許可するため none。 */
  token_endpoint_auth_methods_supported?: OidcConfigurationTokenEndpointAuthMethodsSupportedItem[];
  /** PKCE は S256 のみ。plain は許可しない。 */
  code_challenge_methods_supported: OidcConfigurationCodeChallengeMethodsSupportedItem[];
  claims_supported?: string[];
}
