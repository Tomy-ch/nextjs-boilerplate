# SonarQube Cloud の解析設定。このリポジトリの SonarCloud organization に紐づくため、
# `.github/workflows/sonarcloud.yaml` と併せて剥がしの対象になる
# （scripts/setup/remove-boilerplate-only/manifest.ts の SELF_DESTRUCT_PATHS）。
# fork 先で使うなら、自分の organization の projectKey を書いて SONAR_TOKEN を登録する。

sonar.projectKey=Tomy-ch_nextjs-boilerplate
sonar.organization=tomy-ch
sonar.projectName=nextjs-boilerplate

sonar.sources=.
sonar.sourceEncoding=UTF-8

# 生成物と取得物を外す。編集できないものの所見は行動につながらず、直す先は契約か生成器になる。
sonar.exclusions=\
  **/node_modules/**,\
  **/dist/**,\
  **/coverage/**,\
  **/.next/**,\
  **/storybook-static/**,\
  src/adapters/gen/**,\
  mocks/api/**,\
  tokens/generated/**,\
  graphify-out/**,\
  docs/**,\
  pnpm-lock.yaml

sonar.tests=.
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx

# カバレッジのゲートは test ワークフローが 100% の閾値で持つ（ADR 0090）。SonarCloud へ LCOV を
# 渡さない以上、ここで coverage を品質ゲートに入れると「報告が無い」を「不足」として読む。
# 静的解析と重複検出の対象からは外さない。
sonar.coverage.exclusions=**/*

# 抑止（[0110](docs/adr/0110-security-operations.md) §3.4）。各エントリに理由と撤回条件を書く。
#
# S4036「PATH に固定・書き換え不能なディレクトリだけが入っていることを確かめよ」。
# このリポジトリはコマンドを**素の名前で**呼び、解決は mise を活性化した PATH が行う
# （[0003](docs/adr/0003-version-manager.md)）。絶対パスを埋めると、ADR 0003 が禁じている
# 「呼び出しごとの包み込み」そのものになり、手元と CI で綴りが分かれる。指摘の趣旨は
# 満たしている —— PATH を決めるのは runner と mise で、実行するスクリプトではない。
#
# 撤回条件: コマンドの解決を PATH から外す決定（ADR 0003 の改訂）が入ったとき。
sonar.issue.ignore.multicriteria=e1,e2,e3,e4,e5,e6,e7,e8,e9,e10,e11,e12,e13,e14,e15,e16,e17,e18,e19,e20,e21,e22,e23,e24,e25,e26,e27,e28,e29,e30,e31,e32
sonar.issue.ignore.multicriteria.e1.ruleKey=typescript:S4036
sonar.issue.ignore.multicriteria.e1.resourceKey=scripts/**/*.ts

# S5976「複数のテストを parameterized 1 本へまとめよ」。ADR [0090](docs/adr/0090-testing-strategy.md)
# はケースを 1 つずつ名前で立てることを求めており、その名前が失敗の場所を語る。記法の違いを
# 確かめるケース群は入力が似ているだけで、**変わる理由は別々**である。名前テンプレート付きの
# `it.each` は同 ADR が許すので、同じ理由で変わるケースを束ねる用途では引き続き使う。
#
# 撤回条件: ADR 0090 がケースごとの命名を求めなくなったとき。
sonar.issue.ignore.multicriteria.e2.ruleKey=typescript:S5976
sonar.issue.ignore.multicriteria.e2.resourceKey=**/*.test.ts*

# S1192「同じ文字列リテラルの繰り返しを定数へ」。テストの入力と期待値を定数へ畳むと、そのケースが
# 何を与えて何を確かめているのかが、その行から消える。
#
# 撤回条件: 同じリテラルが**同じ理由で**変わる（契約の綴りなど）と言える場所が出たとき。そこは
# 抑止の対象ではなく、素直に定数化する。
sonar.issue.ignore.multicriteria.e3.ruleKey=typescript:S1192
sonar.issue.ignore.multicriteria.e3.resourceKey=**/*.test.ts*

# S2871「`.sort()` に比較関数を渡せ」。**この規則が指示する直し方が、ここでは誤りになる。**
# 挙げられている箇所はすべて文字列の配列で、並べた結果は lockfile の突合・digest・コミットされる
# 生成物へ入る。規則が求める `localeCompare` は引数を省くと実行環境の locale から解決するため、
# 同じ入力が機械によって違う順序になる（`sv-SE` は `ä` を最後に置き、`en-US` は先頭に置く）。
# 素の `.sort()` は UTF-16 コード単位順で、環境に依らない。
#
# 代償を書いておく: この規則が本来捕まえたい `[10, 9, 1].sort()`（数値を文字列として並べる誤り）も、
# 対象のディレクトリでは鳴らなくなる。`src/**` では鳴らしたままにしてある。
#
# 撤回条件: 規則が文字列配列に対して `localeCompare` を求めなくなったとき。
sonar.issue.ignore.multicriteria.e4.ruleKey=typescript:S2871
sonar.issue.ignore.multicriteria.e4.resourceKey=scripts/**/*.ts
sonar.issue.ignore.multicriteria.e5.ruleKey=typescript:S2871
sonar.issue.ignore.multicriteria.e5.resourceKey=baseline/**/*.ts
sonar.issue.ignore.multicriteria.e6.ruleKey=typescript:S2871
sonar.issue.ignore.multicriteria.e6.resourceKey=e2e/**/*.ts
sonar.issue.ignore.multicriteria.e7.ruleKey=typescript:S2871
sonar.issue.ignore.multicriteria.e7.resourceKey=vrt/**/*.ts
sonar.issue.ignore.multicriteria.e8.ruleKey=typescript:S2871
sonar.issue.ignore.multicriteria.e8.resourceKey=tokens/**/*.ts

# S1523「動的なコードの注入・実行が安全か確かめよ」。`runInNewContext` に渡しているのは
# **自分の build が出した成果物**（`.next` の RSC manifest）で、外から来た文字列ではない。
# 読む `_client-reference-manifest.js` は module ではなく `globalThis.__RSC_MANIFEST` へ代入するだけの
# script なので、値を得るには評価するしかない。捨てる context に閉じ込めているのはその代入の行き先で、
# 指摘が心配する「何を実行するか」の側は build 成果物という出所で決まっている。
#
# 撤回条件: 読み込む対象が build 成果物以外へ広がったとき。そこは抑止ではなく、パースし直す先になる。
sonar.issue.ignore.multicriteria.e9.ruleKey=typescript:S1523
sonar.issue.ignore.multicriteria.e9.resourceKey=scripts/bundle-budget/index.ts

# S7631「fork のコードを実行していないか確かめよ」。`workflow_run` は base repository の文脈で
# 書き込み権を持つトークンを握るため、指摘の懸念そのものは正しい。**求められている対策は既に入って
# いる** —— `Refuse fork pull requests` が、どの checkout よりも前に head の repository を突き合わせて
# 落とす。規則は checkout の行だけを見るので、その手前の拒否を読まない。
#
# 撤回条件: fork の拒否を外す決定が入ったとき。そのときは抑止ではなく、設計そのものを問い直す。
sonar.issue.ignore.multicriteria.e10.ruleKey=githubactions:S7631
sonar.issue.ignore.multicriteria.e10.resourceKey=.github/workflows/baseline-retake.yaml

# S5332「http は安全でない。https を使え」。`http://internal.invalid` は URL の解決先を確かめるためだけの
# origin で、接続しない。`.invalid` は名前解決されないことが保証された予約 TLD（RFC 6761）なので、
# https へ替えても意味が変わらないまま「接続しうる形」に見えるだけになる。
#
# 撤回条件: この定数が実際に接続される先になったとき。
sonar.issue.ignore.multicriteria.e11.ruleKey=typescript:S5332
sonar.issue.ignore.multicriteria.e11.resourceKey=src/model/return-url.ts

# S6759「component の props を読み取り専用にせよ」。React の props は契約として読み取り専用で、
# 書き換えても再描画されない以上、`Readonly<>` で包んでも防げる誤りが増えない。React 自身の型も、
# shadcn/ui も、Next.js の文書も包まない —— 従う先はそちらのデファクトに置く。
#
# 撤回条件: props を書き換える誤りが実際に起きたとき。そのときは包む前に、なぜ書けたのかを見る。
sonar.issue.ignore.multicriteria.e12.ruleKey=typescript:S6759
sonar.issue.ignore.multicriteria.e12.resourceKey=**/*.tsx

# S6772「次の要素の前の空白が曖昧」。**日本語の本文では指摘が逆になる。**JSX は要素に隣接する改行を
# 削るので、`、` の直後に置いた要素はそのまま隣接して出る —— それがこの文章の正しい組み方で、
# 指示どおり `{" "}` を入れると読点と強調の間に空白が入る。英語の本文なら指摘のとおりになるが、
# 出力は日本語と決まっている（AGENTS.md § Language Rules）。
#
# 撤回条件: 表示言語に英語が入ったとき。そこは行ごとに見る必要が出る。
sonar.issue.ignore.multicriteria.e13.ruleKey=typescript:S6772
sonar.issue.ignore.multicriteria.e13.resourceKey=**/*.tsx

# S2699「アサーションが 1 つも無い」。ESLint の規則を試すのは `RuleTester.run()` で、valid / invalid
# の食い違いはその中で throw される。`expect` が現れないのは判定を RuleTester へ委ねているためで、
# アサーションが無いわけではない。
#
# 撤回条件: RuleTester を使わない試験がこの位置に増えたとき。
sonar.issue.ignore.multicriteria.e14.ruleKey=typescript:S2699
sonar.issue.ignore.multicriteria.e14.resourceKey=eslint-rules/**/*.test.ts

# S1186「空のメソッド」。jsdom が持たない `ResizeObserver` の身代わりで、3 つとも何もしないのが
# 正しい —— jsdom はレイアウトを持たず、大きさが変わる瞬間そのものが存在しない。理由はクラスの
# 直前に 1 度書いてあり、規則が求めるのは**各ブロックの中**のコメントなので、従うと同じ説明が
# 3 つ増える。
#
# 撤回条件: 身代わりが何かを通知するようになったとき。
sonar.issue.ignore.multicriteria.e15.ruleKey=typescript:S1186
sonar.issue.ignore.multicriteria.e15.resourceKey=vitest.setup.ts

# S1940「反対の演算子を使え」。`!(bytes <= maxBytes)` は**否定であることに意味がある**。上限が
# 数値として届かなければどの比較も成り立たず、`bytes > maxBytes` に書き換えると予算の無い要求が
# 全部通る。同じ趣旨がその場のコメントにも書いてある。
#
# 撤回条件: 上限が数値であることを、この関数より手前で保証するようになったとき。
sonar.issue.ignore.multicriteria.e16.ruleKey=typescript:S1940
sonar.issue.ignore.multicriteria.e16.resourceKey=src/adapters/http/url-budget.ts

# S6564「冗長な型の別名」。`UpdateProductAction` は構造が `CreateProductAction` と同じだが、
# **別の契約に名前を与えている**。畳むと更新の送信先が「作成の型」として現れ、片方だけが動いた
# ときに型が食い違いを示さなくなる。
#
# 撤回条件: 2 つの契約が同じ理由で変わると言えるようになったとき。
sonar.issue.ignore.multicriteria.e17.ruleKey=typescript:S6564
sonar.issue.ignore.multicriteria.e17.resourceKey=src/features/admin/products/form-state.ts

# S7763「`export…from` で再輸出せよ」。ここは再輸出ではなく**契約の値に画面側の名前を与えている**
# 場所で、その名前に TSDoc が付く。`export { x as Y } from` へ畳むと綴りだけが残り、なぜ契約の値を
# そのまま出すのかという説明の置き場が無くなる。
#
# 撤回条件: 説明が要らない、名前も変えない再輸出が現れたとき。
sonar.issue.ignore.multicriteria.e18.ruleKey=typescript:S7763
sonar.issue.ignore.multicriteria.e18.resourceKey=src/adapters/client/api/*.ts

# S7765「`.some()` ではなく `.includes()`」。対象はどちらも `as const` の配列で、要素の型が
# 入力より狭い。`.includes()` は引数に要素の型を要求するため**型検査を通らない** ——
# `.some((x) => x === value)` の形はそれを避けるために書かれている。
#
# 撤回条件: 配列の要素の型が入力と一致するようになったとき。
sonar.issue.ignore.multicriteria.e19.ruleKey=typescript:S7765
sonar.issue.ignore.multicriteria.e19.resourceKey=playwright.config.ts
sonar.issue.ignore.multicriteria.e20.ruleKey=typescript:S7765
sonar.issue.ignore.multicriteria.e20.resourceKey=scripts/lib/test-requirement.ts

# S7758「`codePointAt()` を使え」。指摘は正しく、代理対を 1 文字として読む点で `charCodeAt` より
# 適切になる。ただしこの関数が返すのは mock 応答の種で、**全画面の見た目がこの値から決まる**。
# 替えると基準画像が全数入れ替わり、報告された分だけを撮り直すという撮り直しの前提が崩れる。
#
# 撤回条件: 全数の撮り直しを行う変更と同じ PR に入れられるとき。
sonar.issue.ignore.multicriteria.e21.ruleKey=typescript:S7758
sonar.issue.ignore.multicriteria.e21.resourceKey=mocks/stable-responses.ts

# S6845「`tabIndex` は対話要素にだけ」。スクロールできる領域は非対話でも focus を受けられないと
# keyboard だけで中身を読めず、WCAG 2.1.1 に反する。同じ趣旨で biome 側も既に個別に外してあり、
# その judgement をこちらでも同じ理由で通す。
#
# 撤回条件: 領域そのものが対話要素になったとき。
sonar.issue.ignore.multicriteria.e22.ruleKey=typescript:S6845
sonar.issue.ignore.multicriteria.e22.resourceKey=src/components/**/*.tsx

# S6819「`role="status"` ではなく `<output>`」。`<output>` の暗黙の role は `status` なので
# 支援技術から見た意味は変わらない一方、**描画される要素そのものが変わる**（`<p>` は block、
# `<output>` は inline で、form に紐づく要素でもある）。この repository は画面を画像で固定して
# いるため、意味の変わらない置き換えのために全数の撮り直しを起こすことになる。
#
# 撤回条件: `role="status"` が支援技術に届かない実測が出たとき。そこは見た目の差より優先する。
sonar.issue.ignore.multicriteria.e23.ruleKey=typescript:S6819
sonar.issue.ignore.multicriteria.e23.resourceKey=src/**/*.tsx

# S6850「見出しに内容が要る」。`PageHeaderTitle` は `h1` の属性をそのまま広げる入れ物で、
# 内容は呼び出し元が `children` として渡す。規則はこのファイルだけを見るため、内容が
# 常に空だと読む。
#
# 撤回条件: この component が内容を自分で決めるようになったとき。
sonar.issue.ignore.multicriteria.e24.ruleKey=typescript:S6850
sonar.issue.ignore.multicriteria.e24.resourceKey=src/components/shell/page-header/page-header.tsx

# S3776「認知的複雑度が高い」。`stripMarkers` は 6 つの状態を持つ行のステートマシンで、分岐は
# `depth` / `replaceState` / `cutJustBefore` / `removed` / `out` の 5 つを同時に動かす。補助関数へ
# 割ると、その 5 つを引数と戻り値で往復させることになり、**どの状態がどこで動くのかが読めなくなる**。
# 分岐はそれぞれ固有の診断文を持っており、いま 1 か所に並んでいることが読み筋になっている。
#
# 撤回条件: replace マーカーの状態が増えるとき。そこで表に起こす。
sonar.issue.ignore.multicriteria.e25.ruleKey=typescript:S3776
sonar.issue.ignore.multicriteria.e25.resourceKey=scripts/setup/lib/markers.ts

# S5843「正規表現が複雑すぎる」。`image-reference.ts` の 3 本は、**どの記法を固定の対象と見なすか**
# そのものを定義している。緩めると対象が静かに広がり、締めると対象から静かに外れる —— どちらも
# `unparsedLines` が「対応記法の外」として報告する経路を通らずに起きる。上限 20 に対して 21 で、
# 削れるのは意味を持つ枝だけになる。
#
# 撤回条件: 対象の記法が減ったとき。
sonar.issue.ignore.multicriteria.e26.ruleKey=typescript:S5843
sonar.issue.ignore.multicriteria.e26.resourceKey=scripts/images-pin/image-reference.ts

# S5843「正規表現が複雑すぎる」。`uses-reference.ts` は e26 と同じ理由で、**どの記法を固定の対象と
# 見なすか**を定義している。`uses:` の値から引用符と空白を締め出す枝も、tag コメントを拾う枝も、
# 外すと対象が静かにずれる —— そのずれは `unparsedUsesLines` が「対応記法の外」として報告する
# 経路を通らない。上限 20 に対して 23 で、削れるのは意味を持つ枝だけになる。
#
# 撤回条件: 対象の記法が減ったとき。
sonar.issue.ignore.multicriteria.e27.ruleKey=typescript:S5843
sonar.issue.ignore.multicriteria.e27.resourceKey=scripts/actions-pin/uses-reference.ts

# S8786「後戻りで走査が超線形になる正規表現を簡単にせよ」。**指摘が本物だったものは直してあり、
# ここに残っているのは形だけが該当するもの**である。直したのは、置換されなかった差し込み先の検出
# （`scripts/setup/baseline-store/plan.ts`）、compose の image 参照（`scripts/images-pin/image-reference.ts`）、
# `Host` の値からポートを落とす箇所（`src/adapters/server/auth/development-access.ts`）—— 最後のものだけが
# 要求から届く値を読んでおり、そこは正規表現をやめた。
#
# 残る 5 本が読むのは、いずれも**このリポジトリ自身が作った値**である。作業ツリーへ渡すブランチ名、
# 設定が持つ配信元、ツリーの中のファイルの見出しと行。形は末尾の繰り返しを落とす trim（`-+$` /
# `\/+$`）か、閉じ記号を除いた文字クラス（`[^\]]*\]`）で、書き換えた先は同じことを手書きの
# ループで言い直したものにしかならない。
#
# 撤回条件: これらのどれかが、プロセスの外から届く値を読むようになったとき。そこは抑止ではなく、
# `development-access.ts` と同じく正規表現をやめる先になる。
sonar.issue.ignore.multicriteria.e28.ruleKey=typescript:S8786
sonar.issue.ignore.multicriteria.e28.resourceKey=scripts/review/worktree.ts
sonar.issue.ignore.multicriteria.e29.ruleKey=typescript:S8786
sonar.issue.ignore.multicriteria.e29.resourceKey=scripts/lib/markdown-anchor.ts
sonar.issue.ignore.multicriteria.e30.ruleKey=typescript:S8786
sonar.issue.ignore.multicriteria.e30.resourceKey=scripts/lib/doc-links.ts
sonar.issue.ignore.multicriteria.e31.ruleKey=typescript:S8786
sonar.issue.ignore.multicriteria.e31.resourceKey=scripts/images-pin/image-reference.ts
sonar.issue.ignore.multicriteria.e32.ruleKey=typescript:S8786
sonar.issue.ignore.multicriteria.e32.resourceKey=src/model/media.ts

# 重複の密度。同じ形のケースが並ぶのはテストの性質で、束ねると 1 つを直したい人が呼び出し側を
# 読むことになる。**実装側の重複は対象に残す** —— そちらは同じ理由で変わることが多い。
#
# 撤回条件: テストの重複が、ケースの独立性ではなく単なる書き写しだと言える形で増えたとき。
sonar.cpd.exclusions=**/*.test.ts,**/*.test.tsx
